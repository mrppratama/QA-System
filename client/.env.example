# Supabase project config (safe to expose in the browser — the anon key
# is public by design; access is controlled by Supabase Auth + RLS).
VITE_SUPABASE_URL="https://[ref].supabase.co"
VITE_SUPABASE_ANON_KEY="your_anon_key_here"
